# Custom Agent Rules

- **Git Commit Author Rules**:
  - Always enforce `user.name` as `Suryadeep091` and `user.email` as `suryadeep.singh@azistaindustries.com` for all git commits.
  - Set local config `credential.username` to `Suryadeep091` to prevent other cached credentials (such as `RuLeKiLLeR`) from being used for push/pull.
